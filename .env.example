# Node Environment
NODE_ENV=development

# API Server
PORT=2003

# Frontend
NEXT_PUBLIC_PORT=2004
NEXT_PUBLIC_API_URL=http://localhost:2003

# Database (PostgreSQL 17)
DATABASE_URL="postgresql://meocrm_user:meocrm_dev_password@localhost:2001/meocrm_dev?schema=public"

# Redis (no password for local dev)
REDIS_HOST=localhost
REDIS_PORT=2002

# JWT
JWT_SECRET=jules-vm-secret-key
JWT_REFRESH_SECRET=jules-vm-refresh-secret
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:2004

# API
API_PREFIX=api
API_VERSION=v1

# Prisma
PRISMA_HIDE_UPDATE_MESSAGE=true
